import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    marginTop: 0,
    marginBottom: 16,
    height: 70,
    marginRight: 16,
    padding: 16,
  },
});

export default styles;
