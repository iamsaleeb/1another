import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  profileThoucable: {
    height: 32,
    width: 32,
    position: "absolute",
    right: 16,
  },
});

export default styles;
